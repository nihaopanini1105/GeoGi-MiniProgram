<view class="page">
  <view class="report-hero card">
    <text class="eyebrow">报告结构示例</text>
    <text class="h1">GeoGi GEO 报告</text>
    <text class="lead">这里只说明正式报告会包含哪些章节，不展示虚构分数或示例诊断结论。客户正式报告全部由 GeoGi OS 基于真实证据生成、审核并发布。</text>
  </view>

  <view class="section outline-card card">
    <text class="outline-title">正式报告主要结构</text>
    <view class="outline-row" wx:for="{{outline}}" wx:key="*this">
      <text class="outline-num">{{index + 1}}</text>
      <text>{{item}}</text>
    </view>
  </view>

  <view class="section card">
    <text class="block-title">数据权威边界</text>
    <text class="body">小程序不运行 GEO 诊断、不计算评分、不生成优化建议、不拼接报告正文，只展示 GeoGi OS 已发布的正式交付物。</text>
  </view>

  <button class="primary-button full-button" bindtap="goDiagnosis">提交品牌资料</button>
</view>

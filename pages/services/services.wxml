<view class="page">
  <view class="section-head">
    <text class="eyebrow">SERVICE SYSTEM</text>
    <text class="h1">GeoGi 服务能力</text>
    <text class="lead">围绕品牌在 AI 平台中的可见度、理解度与推荐表现，从诊断到方案与执行逐步推进。</text>
  </view>

  <view class="process card">
    <view class="process-step" wx:for="{{process}}" wx:key="*this">
      <text class="process-index">0{{index + 1}}</text>
      <text class="process-title">{{item}}</text>
    </view>
  </view>

  <view class="section list">
    <view class="service-card card" wx:for="{{services}}" wx:key="title">
      <view class="service-top">
        <text class="pill">{{item.label}}</text>
      </view>
      <text class="service-title">{{item.title}}</text>
      <text class="body">{{item.desc}}</text>
      <view class="point-list">
        <view class="point-item" wx:for="{{item.points}}" wx:for-item="point" wx:key="*this">
          <view class="dot"></view>
          <text>{{point}}</text>
        </view>
      </view>
    </view>
  </view>

  <view class="section action-panel">
    <text class="action-title">不确定从哪里开始？</text>
    <text class="action-copy">先提交品牌初步诊断，确认当前最值得优先解决的问题。</text>
    <button class="primary-button" bindtap="goDiagnosis">开始品牌初步诊断</button>
    <button class="secondary-button" bindtap="goContact">获取 GEO 优化建议</button>
  </view>
</view>
